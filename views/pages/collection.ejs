<!DOCTYPE html>
<html lang="en">
<% include ../templates/head %>
<body>
  <% collection.map(book => { %>
    <div><%= book.title %> </div>
    <div><%= book.author %> </div>
    <div><%= book.comments %> </div>

  <% }) %>
<% include ../templates/footer %>
</body>
</html>